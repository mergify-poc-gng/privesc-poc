queue_rules:
  - name: default
    queue_conditions:
      - label=race
    merge_conditions:
      - check-success=gate
pull_request_rules:
  - name: q
    conditions:
      - label=race
    actions:
      queue:
        name: default
