queue_rules:
  - name: default
    merge_method: fast-forward
    merge_conditions:
      - label=go
pull_request_rules:
  - name: q
    conditions:
      - base=main
      - label=queueme
    actions:
      queue:
        name: default
