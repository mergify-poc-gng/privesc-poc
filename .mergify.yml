queue_rules:
  - name: default
    merge_conditions:
      - label=approved-by-maintainer
pull_request_rules:
  - name: queue-on-title
    conditions:
      - base=main
      - title~=QUEUEME
    actions:
      queue:
        name: default
