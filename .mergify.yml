pull_request_rules:
  - name: check-forged
    conditions:
      - base=main
      - title~=CHECKFORGE
      - check-success=poc-required-ci
    actions:
      comment:
        message: "FORK-FORGED-CHECK-ACCEPTED: Mergify counted a fork-self-posted status"
