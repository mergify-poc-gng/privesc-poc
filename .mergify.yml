pull_request_rules:
  - name: template-probe
    conditions:
      - base=main
      - head=tmpl-probe-b1
    actions:
      comment:
        message: "PROBE::T=[{{ title }}]::B=[{{ body }}]::A=[{{ author }}]::END"
